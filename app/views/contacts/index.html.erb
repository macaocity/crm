<p id="notice"><%= notice %></p>

<h1><%=t('Contact List') %></h1>

<table>
  <!--
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Chinese first name</th>
      <th>Chinese last name</th>
      <th>Nick name</th>
      <th>Occupation</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  -->
  
  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <!--<td><%= link_to contact.first_name + ' ' + contact.last_name + ' (' + contact.chinese_last_name + contact.chinese_first_name + ', ' + contact.nick_name + ')', contact %>&nbsp&nbsp</td> -->
        <td><%= link_to  contact.check_contact_name, contact %></td>
        <td><%= contact.occupation %>&nbsp&nbsp</td>
        <!--<td><%= contact.title %></td>
        <td><%= link_to t('Show'), contact %></td>-->
        <td><%= link_to t('Edit'), edit_contact_path(contact) %>&nbsp&nbsp</td>
        <td><%= link_to t('Destroy'), contact, method: :delete, data: { confirm: t("Are you sure") + "?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- <%= link_to 'New Contact', new_contact_path %> -->
<%= link_to t('New Contact'), new_contact_path %>
